name: Keep App Awake

on:
  schedule:
    - cron: "*/15 * * * *" # Runs every 15 minutes
  workflow_dispatch: # Allows manual triggering

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping Koyeb App
        run: curl -sSf https://wily-georgeanne-zelo-assistant-166036d4.koyeb.app/ > /dev/null
